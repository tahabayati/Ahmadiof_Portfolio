import styles from './instagram.module.css';

export default function Instagram(){
  return(
    <section id="instagram" className={`section ${styles.section}`}>
      Instagram
    </section>
  );
}
